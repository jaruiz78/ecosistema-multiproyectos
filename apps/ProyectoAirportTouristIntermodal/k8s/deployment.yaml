apiVersion: apps/v1
kind: Deployment
metadata:
  name: proyecto-airport-tourist-intermodal
  namespace: default
  labels:
    app: proyecto-airport-tourist-intermodal
spec:
  replicas: 2
  selector:
    matchLabels:
      app: proyecto-airport-tourist-intermodal
  template:
    metadata:
      labels:
        app: proyecto-airport-tourist-intermodal
    spec:
      containers:
        - name: proyecto-airport-tourist-intermodal
          image: gcr.io/corp-ecosystem/proyecto-airport-tourist-intermodal:latest
          resources:
            requests:
              memory: "256Mi"
              cpu: "200m"
            limits:
              memory: "512Mi"
              cpu: "1000m"
          ports:
            - containerPort: 8080
---
apiVersion: v1
kind: Service
metadata:
  name: proyecto-airport-tourist-intermodal-service
  namespace: default
spec:
  selector:
    app: proyecto-airport-tourist-intermodal
  ports:
    - protocol: TCP
      port: 80
      targetPort: 8080
