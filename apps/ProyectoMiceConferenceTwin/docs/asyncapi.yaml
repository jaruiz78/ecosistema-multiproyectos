asyncapi: 3.0.0
info:
  title: ProyectoMiceConferenceTwin Event Stream
  version: 1.0.0
  description: Transmisión de eventos reactivos y telemetría para ProyectoMiceConferenceTwin
channels:
  telemetryEvents:
    address: 'corp.proyectomiceconferencetwin.events'
    messages:
      telemetryMessage:
        payload:
          type: object
          properties:
            tenantId:
              type: string
            timestamp:
              type: string
              format: date-time
