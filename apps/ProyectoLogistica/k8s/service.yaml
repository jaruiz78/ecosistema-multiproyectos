apiVersion: serving.knative.dev/v1
kind: Service
metadata:
  name: proyecto-logistica
spec:
  template:
    spec:
      containers:
        - image: gcr.io/proyecto/logistica:latest
          resources:
            limits:
              memory: 512Mi
