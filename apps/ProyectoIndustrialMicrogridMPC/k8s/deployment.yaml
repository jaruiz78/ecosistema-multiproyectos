apiVersion: apps/v1
kind: Deployment
metadata:
  name: proyecto-industrial-microgrid-mpc
  namespace: default
  labels:
    app: proyecto-industrial-microgrid-mpc
spec:
  replicas: 2
  selector:
    matchLabels:
      app: proyecto-industrial-microgrid-mpc
  template:
    metadata:
      labels:
        app: proyecto-industrial-microgrid-mpc
    spec:
      containers:
        - name: proyecto-industrial-microgrid-mpc
          image: gcr.io/corp-ecosystem/proyecto-industrial-microgrid-mpc:latest
          resources:
            requests:
              memory: "256Mi"
              cpu: "200m"
            limits:
              memory: "512Mi"
              cpu: "1000m"
          ports:
            - containerPort: 8080
---
apiVersion: v1
kind: Service
metadata:
  name: proyecto-industrial-microgrid-mpc-service
  namespace: default
spec:
  selector:
    app: proyecto-industrial-microgrid-mpc
  ports:
    - protocol: TCP
      port: 80
      targetPort: 8080
