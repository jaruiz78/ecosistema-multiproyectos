apiVersion: apps/v1
kind: Deployment
metadata:
  name: proyecto-playas-inteligentes-costas
  namespace: default
  labels:
    app: proyecto-playas-inteligentes-costas
spec:
  replicas: 2
  selector:
    matchLabels:
      app: proyecto-playas-inteligentes-costas
  template:
    metadata:
      labels:
        app: proyecto-playas-inteligentes-costas
    spec:
      containers:
        - name: proyecto-playas-inteligentes-costas
          image: gcr.io/corp-ecosystem/proyecto-playas-inteligentes-costas:latest
          resources:
            requests:
              memory: "256Mi"
              cpu: "200m"
            limits:
              memory: "512Mi"
              cpu: "1000m"
          ports:
            - containerPort: 8080
---
apiVersion: v1
kind: Service
metadata:
  name: proyecto-playas-inteligentes-costas-service
  namespace: default
spec:
  selector:
    app: proyecto-playas-inteligentes-costas
  ports:
    - protocol: TCP
      port: 80
      targetPort: 8080
