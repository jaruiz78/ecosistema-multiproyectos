apiVersion: apps/v1
kind: Deployment
metadata:
  name: proyecto-circular-textile-dpp
  namespace: default
  labels:
    app: proyecto-circular-textile-dpp
spec:
  replicas: 2
  selector:
    matchLabels:
      app: proyecto-circular-textile-dpp
  template:
    metadata:
      labels:
        app: proyecto-circular-textile-dpp
    spec:
      containers:
        - name: proyecto-circular-textile-dpp
          image: gcr.io/corp-ecosystem/proyecto-circular-textile-dpp:latest
          resources:
            requests:
              memory: "256Mi"
              cpu: "200m"
            limits:
              memory: "512Mi"
              cpu: "1000m"
          ports:
            - containerPort: 8080
---
apiVersion: v1
kind: Service
metadata:
  name: proyecto-circular-textile-dpp-service
  namespace: default
spec:
  selector:
    app: proyecto-circular-textile-dpp
  ports:
    - protocol: TCP
      port: 80
      targetPort: 8080
