apiVersion: apps/v1
kind: Deployment
metadata:
  name: proyecto-sub-surface-geo-twin
  namespace: default
  labels:
    app: proyecto-sub-surface-geo-twin
spec:
  replicas: 2
  selector:
    matchLabels:
      app: proyecto-sub-surface-geo-twin
  template:
    metadata:
      labels:
        app: proyecto-sub-surface-geo-twin
    spec:
      containers:
        - name: proyecto-sub-surface-geo-twin
          image: gcr.io/corp-ecosystem/proyecto-sub-surface-geo-twin:latest
          resources:
            requests:
              memory: "256Mi"
              cpu: "200m"
            limits:
              memory: "512Mi"
              cpu: "1000m"
          ports:
            - containerPort: 8080
---
apiVersion: v1
kind: Service
metadata:
  name: proyecto-sub-surface-geo-twin-service
  namespace: default
spec:
  selector:
    app: proyecto-sub-surface-geo-twin
  ports:
    - protocol: TCP
      port: 80
      targetPort: 8080
