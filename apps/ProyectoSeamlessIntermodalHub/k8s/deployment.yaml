apiVersion: apps/v1
kind: Deployment
metadata:
  name: proyecto-seamless-intermodal-hub
  namespace: default
  labels:
    app: proyecto-seamless-intermodal-hub
spec:
  replicas: 2
  selector:
    matchLabels:
      app: proyecto-seamless-intermodal-hub
  template:
    metadata:
      labels:
        app: proyecto-seamless-intermodal-hub
    spec:
      containers:
        - name: proyecto-seamless-intermodal-hub
          image: gcr.io/corp-ecosystem/proyecto-seamless-intermodal-hub:latest
          resources:
            requests:
              memory: "256Mi"
              cpu: "200m"
            limits:
              memory: "512Mi"
              cpu: "1000m"
          ports:
            - containerPort: 8080
---
apiVersion: v1
kind: Service
metadata:
  name: proyecto-seamless-intermodal-hub-service
  namespace: default
spec:
  selector:
    app: proyecto-seamless-intermodal-hub
  ports:
    - protocol: TCP
      port: 80
      targetPort: 8080
