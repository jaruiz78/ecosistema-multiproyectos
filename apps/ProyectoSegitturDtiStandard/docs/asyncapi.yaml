asyncapi: 3.0.0
info:
  title: ProyectoSegitturDtiStandard Event Stream
  version: 1.0.0
  description: Transmisión de eventos reactivos y telemetría para ProyectoSegitturDtiStandard
channels:
  telemetryEvents:
    address: 'corp.proyectosegitturdtistandard.events'
    messages:
      telemetryMessage:
        payload:
          type: object
          properties:
            tenantId:
              type: string
            timestamp:
              type: string
              format: date-time
