apiVersion: apps/v1
kind: Deployment
metadata:
  name: proyecto-astroturismo-starlight
  namespace: default
  labels:
    app: proyecto-astroturismo-starlight
spec:
  replicas: 2
  selector:
    matchLabels:
      app: proyecto-astroturismo-starlight
  template:
    metadata:
      labels:
        app: proyecto-astroturismo-starlight
    spec:
      containers:
        - name: proyecto-astroturismo-starlight
          image: gcr.io/corp-ecosystem/proyecto-astroturismo-starlight:latest
          resources:
            requests:
              memory: "256Mi"
              cpu: "200m"
            limits:
              memory: "512Mi"
              cpu: "1000m"
          ports:
            - containerPort: 8080
---
apiVersion: v1
kind: Service
metadata:
  name: proyecto-astroturismo-starlight-service
  namespace: default
spec:
  selector:
    app: proyecto-astroturismo-starlight
  ports:
    - protocol: TCP
      port: 80
      targetPort: 8080
