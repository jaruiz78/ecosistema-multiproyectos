apiVersion: apps/v1
kind: Deployment
metadata:
  name: proyecto-red-paradores-twin
  namespace: default
  labels:
    app: proyecto-red-paradores-twin
spec:
  replicas: 2
  selector:
    matchLabels:
      app: proyecto-red-paradores-twin
  template:
    metadata:
      labels:
        app: proyecto-red-paradores-twin
    spec:
      containers:
        - name: proyecto-red-paradores-twin
          image: gcr.io/corp-ecosystem/proyecto-red-paradores-twin:latest
          resources:
            requests:
              memory: "256Mi"
              cpu: "200m"
            limits:
              memory: "512Mi"
              cpu: "1000m"
          ports:
            - containerPort: 8080
---
apiVersion: v1
kind: Service
metadata:
  name: proyecto-red-paradores-twin-service
  namespace: default
spec:
  selector:
    app: proyecto-red-paradores-twin
  ports:
    - protocol: TCP
      port: 80
      targetPort: 8080
