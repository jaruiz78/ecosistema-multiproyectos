apiVersion: apps/v1
kind: Deployment
metadata:
  name: proyecto-turismo-termal-balnearios
  namespace: default
  labels:
    app: proyecto-turismo-termal-balnearios
spec:
  replicas: 2
  selector:
    matchLabels:
      app: proyecto-turismo-termal-balnearios
  template:
    metadata:
      labels:
        app: proyecto-turismo-termal-balnearios
    spec:
      containers:
        - name: proyecto-turismo-termal-balnearios
          image: gcr.io/corp-ecosystem/proyecto-turismo-termal-balnearios:latest
          resources:
            requests:
              memory: "256Mi"
              cpu: "200m"
            limits:
              memory: "512Mi"
              cpu: "1000m"
          ports:
            - containerPort: 8080
---
apiVersion: v1
kind: Service
metadata:
  name: proyecto-turismo-termal-balnearios-service
  namespace: default
spec:
  selector:
    app: proyecto-turismo-termal-balnearios
  ports:
    - protocol: TCP
      port: 80
      targetPort: 8080
