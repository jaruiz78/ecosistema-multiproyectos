apiVersion: apps/v1
kind: Deployment
metadata:
  name: proyecto-green-hydrogen-desal
  namespace: default
  labels:
    app: proyecto-green-hydrogen-desal
spec:
  replicas: 2
  selector:
    matchLabels:
      app: proyecto-green-hydrogen-desal
  template:
    metadata:
      labels:
        app: proyecto-green-hydrogen-desal
    spec:
      containers:
        - name: proyecto-green-hydrogen-desal
          image: gcr.io/corp-ecosystem/proyecto-green-hydrogen-desal:latest
          resources:
            requests:
              memory: "256Mi"
              cpu: "200m"
            limits:
              memory: "512Mi"
              cpu: "1000m"
          ports:
            - containerPort: 8080
---
apiVersion: v1
kind: Service
metadata:
  name: proyecto-green-hydrogen-desal-service
  namespace: default
spec:
  selector:
    app: proyecto-green-hydrogen-desal
  ports:
    - protocol: TCP
      port: 80
      targetPort: 8080
