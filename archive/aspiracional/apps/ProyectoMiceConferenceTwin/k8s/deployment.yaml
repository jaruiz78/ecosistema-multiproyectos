apiVersion: apps/v1
kind: Deployment
metadata:
  name: proyecto-mice-conference-twin
  namespace: default
  labels:
    app: proyecto-mice-conference-twin
spec:
  replicas: 2
  selector:
    matchLabels:
      app: proyecto-mice-conference-twin
  template:
    metadata:
      labels:
        app: proyecto-mice-conference-twin
    spec:
      containers:
        - name: proyecto-mice-conference-twin
          image: gcr.io/corp-ecosystem/proyecto-mice-conference-twin:latest
          resources:
            requests:
              memory: "256Mi"
              cpu: "200m"
            limits:
              memory: "512Mi"
              cpu: "1000m"
          ports:
            - containerPort: 8080
---
apiVersion: v1
kind: Service
metadata:
  name: proyecto-mice-conference-twin-service
  namespace: default
spec:
  selector:
    app: proyecto-mice-conference-twin
  ports:
    - protocol: TCP
      port: 80
      targetPort: 8080
