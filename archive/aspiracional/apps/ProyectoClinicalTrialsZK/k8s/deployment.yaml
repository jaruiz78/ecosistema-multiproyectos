apiVersion: apps/v1
kind: Deployment
metadata:
  name: proyecto-clinical-trials-zk
  namespace: default
  labels:
    app: proyecto-clinical-trials-zk
spec:
  replicas: 2
  selector:
    matchLabels:
      app: proyecto-clinical-trials-zk
  template:
    metadata:
      labels:
        app: proyecto-clinical-trials-zk
    spec:
      containers:
        - name: proyecto-clinical-trials-zk
          image: gcr.io/corp-ecosystem/proyecto-clinical-trials-zk:latest
          resources:
            requests:
              memory: "256Mi"
              cpu: "200m"
            limits:
              memory: "512Mi"
              cpu: "1000m"
          ports:
            - containerPort: 8080
---
apiVersion: v1
kind: Service
metadata:
  name: proyecto-clinical-trials-zk-service
  namespace: default
spec:
  selector:
    app: proyecto-clinical-trials-zk
  ports:
    - protocol: TCP
      port: 80
      targetPort: 8080
